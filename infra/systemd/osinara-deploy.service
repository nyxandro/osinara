[Unit]
Description=Osinara approved production release deployment
After=docker.service network-online.target
Wants=docker.service network-online.target

[Service]
Type=oneshot
User=root
Group=root
WorkingDirectory=/opt/osinara
EnvironmentFile=/opt/osinara/.env
ExecStart=/opt/osinara/bin/production-deploy.sh
TimeoutStartSec=45min
UMask=0077
NoNewPrivileges=true
PrivateTmp=true
ProtectHome=read-only
ProtectSystem=strict
# The leading dash keeps monitoring optional: a host without the collector still deploys.
ReadWritePaths=/opt/osinara /run/lock -/var/lib/monitoring-agent/textfile

[Install]
WantedBy=multi-user.target
